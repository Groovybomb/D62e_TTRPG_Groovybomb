---
description: Display all pending to-do items from WORK_LOG.md
---

Read `WORK_LOG.md` in the current project directory. Find every unchecked item — lines starting with `- [ ]` — across all sections. Group them by section heading and display them as a clean numbered list under each section heading. Do not display checked items (`- [x]`). At the top, show a total count of open items. Keep descriptions concise — one line each.
